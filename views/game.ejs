<%- include('partial/header') %>

<h2 class="text-center my-4 bg-dark">Jeu du Morpion</h2>

<div
  id="game-board"
  class="d-flex flex-wrap justify-content-center mx-auto"
  style="width: 300px"
>
  <% for (let i = 0; i < 9; i++) { %>
  <div
    class="cell border d-flex align-items-center justify-content-center"
    data-index="<%= i %>"
  ></div>
  <% } %>
</div>

<p class="text-center mt-4 fw-bold" id="status">C’est au tour de X</p>

<div class="text-center mt-3">
  <button class="btn btn-primary" onclick="resetGame()">Rejouer</button>
</div>

<script src="/js/script.js"></script>

<%- include('partial/footer') %>
